import Register from "../../../components/auth/signup/index";
import AppTemplate from "../../../components/app-template";
const RegisterPage = () => {
  return (
    <AppTemplate pageTitle={"Register"}>
      <Register />
    </AppTemplate>
  );
};

export default RegisterPage;
